<!DOCTYPE html>
<html>
<head>
    <%- include('../partials/master_head') %>
    <link rel="stylesheet" href="/css/quiz.css">
    <link rel="stylesheet" href="/css/login.css">
    <link rel="stylesheet" href="/css/user.css">
</head>
<body>
    <div class="main_square">
      <div class="Quiz_Title">
        회원가입
      </div>

      <form action="/users/register" method="post">
      <div class="login_Wrap">

        <div class="input_Wrap">
          <div class="blue_box">
            닉네임 <span style="color:#3CC9FF;font-weight:700;">|</span>
          </div>
          <input type="text" id="nickname" name="nickname" class="answer user_input" placeholder="닉네임을 입력하세요." value="<%= user.nickname %>" <%= (errors.nickname)?'is-invalid':'' %>">
        </div>

        <div class="input_Wrap">
          <div class="blue_box">
            아이디 <span style="color:#3CC9FF;font-weight:700;">|</span>
          </div>
          <input type="text" id="user_id" name="user_id" class="answer user_input" placeholder="아이디를 입력하세요." value="<%= user.user_id %>" <%= (errors.user_id)?'is-invalid':'' %>">
        </div>

        <div class="input_Wrap">
          <div class="blue_box">
            비밀번호 <span style="color:#3CC9FF;font-weight:700;">|</span>
          </div>
          <input type="password" id="password" name="password" class="answer user_input" placeholder="비밀번호 입력하세요." <%= (errors.password)?'is-invalid':'' %>">
        </div>

        <div class="input_Wrap">
          <div class="blue_box">
            비번 확인 <span style="color:#3CC9FF;font-weight:700;">|</span>
          </div>
          <input type="password" id="passwordConfirmation" name="passwordConfirmation" class="answer user_input" placeholder="비밀번호 확인" <%= (errors.passwordConfirmation)?'is-invalid':'' %>">
        </div>

        <div class="input_Wrap">
          <div class="blue_box">
            이메일 <span style="color:#3CC9FF;font-weight:700;">|</span>
          </div>
          <input type="email" id="email" name="email" class="answer user_input" placeholder="이메일" value="<%= user.email %>" <%= (errors.email)?'is-invalid':'' %>">
        </div>

        <input type="number" id="score" name="score" value="0" style="display:none;" readonly>

        <!-- 메시지는 하단에다 한 번에 표시해주려고 합니다 -->
        <% if(errors.nickname){ %>
          <div class="invalid-feedback"><%= errors.nickname.message %></div>
        <% } %>
        <% if(errors.user_id){ %>
          <div class="invalid-feedback"><%= errors.user_id.message %></div>
        <% } %>
        <% if(errors.password){ %>
          <div class="invalid-feedback"><%= errors.password.message %></div>
        <% } %>
        <% if(errors.passwordConfirmation){ %>
          <div class="invalid-feedback"><%= errors.passwordConfirmation.message %></div>
        <% } %>
        <% if(errors.email){ %>
          <div class="invalid-feedback"><%= errors.email.message %></div>
        <% } %>

        <% if(errors.unhandled){ %>
          <div>
            <%= errors.unhandled %>
          </div>
        <% } %>
      </div>
      <button class="Btn" style="width:20%;" type="submit">회원가입</button>
    </form>
      <a href="/users" class="change">로그인하기</a>
    </div>
</body>
</html>
